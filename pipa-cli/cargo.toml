[package]
name = "pipa-cli"
version = "0.1.0"
edition = "2021"
authors = ["Daniel Wallace"]
description = "Command-line interface for the pipa audit engine"
repository = "https://github.com/developers/lokryn-pipeaudit-core"
license = "MIT OR Apache-2.0"
keywords = ["cli", "audit", "compliance", "data-quality"]

[dependencies]
# Pull in the core engine from the sibling crate
pipa = { path = "../pipa" }

# CLI-specific dependencies
clap = { version = "4.5.48", features = ["derive"] }
tokio = { version = "1.47.1", features = ["full"] }
whoami = "1.6.1"
hostname = "0.4.1"
dotenv = "0.15"
glob = "0.3.3"

[dev-dependencies]
pretty_assertions = "1.4"

[[bin]]
name = "pipa"
path = "src/main.rs"
